############################################################
# 🧱 Node.js build / dépendances
############################################################
node_modules/              # dépendances installées par npm
dist/                      # dossiers de build ou transpilation
build/
coverage/                  # rapports de tests / coverage

# caches / stores / logs
.pnpm-store/
.cache/
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

############################################################
# 🔐 Fichiers d'environnement / secrets
############################################################
.env                       # variables sensibles locales
.env.local
.env.*.local
# modèles d'exemple autorisés :
!.env.example
!.env.sample

############################################################
# 🗃️ Bases de données / exports volumineux
############################################################
db/*.sql                   # dumps ou exports manuels
!db/schema.sql             # exceptions : structure SQL de référence
!db/seed.sql               # exceptions : données de départ légères
# si tu veux garder ton script d'initialisation principal :
!db/revision_db.sql

############################################################
# 🧩 Systèmes / OS / IDE
############################################################
.DS_Store                  # macOS
Thumbs.db                  # Windows
*.swp                      # fichiers temporaires vim/nano

.vscode/                   # préférences VS Code locales
.idea/                     # projets JetBrains
*.iml

############################################################
# 🚀 Déploiement (optionnel)
############################################################
# fichiers générés ou spécifiques à un hébergeur
logs/
tmp/
.env.production.local
.env.staging.local

# Infomaniak / Node Hosting (logs ou tmp éventuels)
.ekomi/
.kDeploy/
